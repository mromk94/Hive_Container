# Native Plugins

Kotlin/Swift native plugins for OMK Container (e.g., secure key storage, TFLite runners, system-level hooks).

This folder is a placeholder; actual plugins will be generated and wired into the Flutter app via platform channels or FFI.
